# Netlify build configuration for the Choisisseur PWA.
[build]
  command = "npm run build"
  publish = "dist"

# Ensure the service worker is always revalidated so PWA updates ship promptly.
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
